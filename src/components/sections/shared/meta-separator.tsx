import { Separator } from "@/components/ui";

/** Divider between inline metadata items in a definition list row. */
export function MetaSeparator() {
  return <Separator className="h-4 self-center" />;
}
